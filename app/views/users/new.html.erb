<%= provide(:title, 'Sign up') %>

<div class="container">
  <div class="col-md-4">
<h1>Sign up</h1>
    <%= form_for(@user) do |f| %>
      <%= render 'fields', :f => f %> 
      <%= f.submit "Create my account", :class => "btn btn-small btn-primary" %>
    <% end %>
  </div>
</div>
